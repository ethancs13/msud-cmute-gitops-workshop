apiVersion: v1
kind: Service
metadata:
  name: paper
  labels:
    app: paper
spec:
  type: ClusterIP
  selector:
    app: paper
  ports:
    - name: minecraft
      port: 25565
      targetPort: 25565
